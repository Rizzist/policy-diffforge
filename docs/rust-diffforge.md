# rust-diffforge Policy Mirror

Canonical graph: `policy-diffforge/projects/rust-diffforge/graph.json`

`rust-diffforge` was detected as an empty project directory when this PolicyGraph was created. Add policy nodes when source files, manifests, commands, or behavior are introduced.
